<block>
    <view class="index">
        <at-tabs current="{{current}}" __triggerObserer="{{ _triggerObserer }}"
        tabList="{{anonymousState__temp}}" bindonclick="handleClick" __fn_onClick="{{true}}">
            <at-tabs-pane current="{{current}}" __triggerObserer="{{ _triggerObserer }}"
            index="{{0}}">
                <view>标签页一的内容</view>
            </at-tabs-pane>
            <at-tabs-pane current="{{current}}" __triggerObserer="{{ _triggerObserer }}"
            index="{{1}}">
                <view>
                    <at-list __triggerObserer="{{ _triggerObserer }}">
                        <block wx:if="{{jokes}}">
                            <at-list-item title="{{joke.title}}" __triggerObserer="{{ _triggerObserer }}"
                            bindonclick="handleClick" wx:for="{{jokes}}" wx:for-item="joke" __fn_onClick="{{true}}"></at-list-item>
                        </block>
                    </at-list>
                </view>
            </at-tabs-pane>
            <at-tabs-pane current="{{current}}" __triggerObserer="{{ _triggerObserer }}"
            index="{{2}}">
                <view>标签页三的内容</view>
            </at-tabs-pane>
        </at-tabs>
        <at-tab-bar fixed="{{true}}" __triggerObserer="{{ _triggerObserer }}"
        tabList="{{anonymousState__temp2}}" bindonclick="handleClickTabBar" current="{{currentTabBar}}"
        __fn_onClick="{{true}}"></at-tab-bar>
    </view>
</block>